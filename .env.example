# Gateway key — clients authenticate to localrouter with this.
# (Only needed if you don't set gateway.apiKey in config.yaml.)
GATEWAY_API_KEY=change-me

# Provider keys referenced from config.yaml via ${VAR}
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
MISTRAL_API_KEY=
